# 使用Node.js官方镜像作为基础镜像
FROM node:16-alpine

# 设置工作目录
WORKDIR /app

# 复制package.json和package-lock.json
COPY package*.json ./

# 安装依赖
RUN npm install

# 复制所有源代码
COPY . .

# 暴露开发服务器端口
EXPOSE 5173

# 运行开发服务器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
